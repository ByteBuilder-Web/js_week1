// old style of concatenating strings

const name = "John";
const age = 20;
const isStudent = true;

console.log("Name: " + name + ", Age: " + age + ", Is Student: " + isStudent);

// new style of concatenating strings, this is call string template

console.log(`Name: ${name}, Age: ${age}, Is Student: ${isStudent}`);